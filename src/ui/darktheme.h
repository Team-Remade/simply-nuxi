#pragma once

#include <wx/wx.h>

namespace DarkTheme
{
    // Background colors
    const wxColour Background = wxColour(30, 30, 30);
    const wxColour PanelBackground = wxColour(30, 30, 30);
    const wxColour OpenGLBackground = wxColour(0.12f, 0.12f, 0.12f, 1.0f);

    // Text colors
    const wxColour Text = wxColour(220, 220, 220);

    // Accent colors
    const wxColour Accent = wxColour(0, 120, 215);
}